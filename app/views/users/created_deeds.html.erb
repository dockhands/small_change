
<div class="container">  

  <% if @created_deeds == [] %>
    <div class="row justify-content-center">
        <p> No deeds created yet...</p>
    </div>

  <% else %>
  
   <%= render 'deed_index'%>

    <% end %>